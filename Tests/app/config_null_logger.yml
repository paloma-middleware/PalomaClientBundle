framework:
    secret: TestSecret
    session:
        storage_id: session.storage.mock_file

paloma_client:
    base_url: 'https://palomatest/api'
    api_key: TestApiKey
    shop_client_logger: ~
    success_log_format: ~
    error_log_format:  ~
    cache_provider: ~
