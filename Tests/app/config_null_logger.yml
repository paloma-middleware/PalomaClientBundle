framework:
    secret: TestSecret
    session:
        storage_id: session.storage.mock_file

paloma_client:
    base_url: 'https://palomatest/api'
    api_key: TestApiKey
    shop_client_logger: ~
    log_format_success: ~
    log_format_failure:  ~
    cache_provider: ~
